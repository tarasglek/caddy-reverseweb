:9877 {
	bind 0.0.0.0
	# sleep is important otherwise caddy might try to connect to python before its listening
	cgi * /bin/sh -c "(sleep 1 && echo http://127.0.0.1:8001) & exec python3 -m http.server 8001" {
		mode proxy
		port 8001
	}
}
